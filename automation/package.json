{
  "name": "@simpleaudience/automation",
  "version": "0.1.0",
  "description": "Trace catalogs and remote-control metadata for text-to-audience flows",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "build:map": "tsx src/scripts/build-button-map.ts",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
